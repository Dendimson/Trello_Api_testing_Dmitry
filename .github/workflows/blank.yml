name: CI

on:

  schedule:
    - cron: '30 5 * * 1,3'
    
  push:
    branches: [ "main" ]

  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Run a one-line script
        run: echo Hello, world!
